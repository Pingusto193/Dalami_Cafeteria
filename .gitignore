# dependências
node_modules/
.pnp
.pnp.*

# Next.js
/.next/
/out/
/build
next-env.d.ts

# Prisma — client gerado (nunca versionar, é recriado por `prisma generate`)
/generated/
/prisma/generated/
/src/generated/

# variáveis de ambiente — nunca versionar segredos
# O padrão é amplo de propósito: .env.bak, .env.old e afins também carregam
# credenciais, e a regra antiga só pegava o `.env` exato.
.env
.env.*
!.env.example

# uploads do adaptador de storage local (dev)
/public/uploads/
/uploads/

# TypeScript
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# sistema operacional
.DS_Store
Thumbs.db
desktop.ini
*.pem

# deploy
.vercel

# pacote da skill Site de 10K, ferramenta de apoio e nao codigo do projeto
site-de-10k - Copia/
